import SwiftUI

/// Placeholder kept for Stage 3 feature branch — not used at runtime.
/// RCTAppDelegate in AppDelegate.swift owns the window root directly.
struct ReactNativeView: View {
    var body: some View { EmptyView() }
}
