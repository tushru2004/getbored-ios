export NODE_BINARY=$(command -v node)

# This project's Xcode project lives at the repo root (not inside an `ios/`
# subfolder), so react-native-xcode.sh's default `PROJECT_ROOT="$PROJECT_DIR/.."`
# resolves one level too high and can't find node_modules / index.js.
export PROJECT_ROOT="$SRCROOT"
